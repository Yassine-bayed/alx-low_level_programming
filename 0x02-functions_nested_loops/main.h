#ifndef MAIN_H
#define MAIN_H
/**
 * Description :
 * ifndef : check if the main.h is already defined or not
 * in case the ifndef is TRUE the define is skipped
 *
 * the declarations below represnt the prototype of functions
 */
int _putchar(char c);
void print_alphabet(void);
void print_alphabet_x10(void);
#endif
